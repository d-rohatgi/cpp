# Suggested Datasets

Keep small samples in git. Keep large data out of git.

Recommended inputs:
- word list for trie/autocomplete
- random integers for heap and algorithm benchmarks
- graph datasets for traversal and shortest path
- text corpora for indexing / query-engine work
- synthetic streams for event processing
